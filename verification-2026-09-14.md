# 线上验收记录 · 2026-09-14

## 已确认

- 用户已确认知乎登录成功；服务器曾确认存在有效登录会话。
- 修复 Cloudflare Workers 不支持 `redirect: error` 的运行时错误，使用 manual 并拒绝异常响应，不携带凭据跟随跳转。
- 更新课题生成提示词：去除旧副本指令叠加；明确 JSON 数组、数量、逐字段长度，保留原有严格校验。
- 明确提示和讲解意图不应产生新题目或评估证据。
- Claude Code 只读复核指出提示词与校验的冲突，已修复上述两项。研究稿逐字引用策略尚未修改，避免放宽证据校验。
- 36 项相关单元测试通过，TypeScript 检查和 Cloudflare 生产构建通过。
- 线上首页、热榜、个人页、会话接口和热榜接口均返回 200（通过诊断代理测试）。

## 尚未完成，不能宣称验收通过

- 新版完整链路（生成课题 → 导师解答 → 小舟迁移追问 → 提示 → 讲解）在生成步骤收到 429，测试已停止，无自动重试。
- 使用项目当前 Access Secret 查询知乎官方额度：直答 TotalQuota=2、TotalUsed=2、RemainingQuota=0。这是查询时状态，不代表以后状态；未在文档保存密钥。
- 未完成新版浏览器交互验收；HTTP 与单元测试不等于真实界面验收。
- workers.dev 在用户网络下曾连接超时；代理可达不等于国内直连可达。尚无用户提供的替代服务器或域名，不进行购买。
- 预设课题只是可查看和编辑的预置内容，不是无接口的实时 AI 替代品。界面明确标注实时讨论仍需 AI。

## 后续执行顺序

1. 额度或服务恢复后，手动运行一次 scripts/check-online.ts；它遇错停止，不重试付费请求。
2. 验证用户回答能触发基于原文的诊断反馈，并用浏览器检查草稿保存、取消、提示和讲解。
3. 用户有空后再处理官方额度问题，以及国内可直接访问的服务器／域名选择；未批准任何购买。
4. 迁移后登记一致的 HTTPS OAuth 回调，再从国内真实网络验收。
