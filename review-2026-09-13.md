# 知行实验室：当前页面与学习逻辑检查

日期：2026-09-13。只检查，未修改产品代码。

## 检查步骤与状态

1. 恢复讨论页：可访问，旧输入草稿保留。当前恢复的是旧黑洞示例，不代表新生成课题的实际文案。
2. 查看首屏与输入区：讨论在笔记之前，但 1265×712 的当前视口首屏看不到输入框；需向下滚动。右侧仍是审稿任务、自评复盘进度，与先解惑的主线冲突。截图已在对话中显示，未完成本地截图归档，因此不是完整视觉审计交付。
3. 检查导师到小舟的衔接：代码显示只有最后一条由导师发言时才出现新情境按钮；导师只做澄清也会出现，小舟结束后却没有同等入口。真实生成效果本轮未测试。
4. 检查提示后回答：用模拟提供方复现，问过有 probe 的问题后，请求提示，提示回复未附 probe 时，下一次含真实原话的 evidence 会被拒绝。不是上游 API 故障。
5. 检查持续讨论与记录：23 条消息起禁用发送并隐藏输入区，约 12 轮即中断，没有继续新一轮的入口。evidence 存在本地但未传入后续模型上下文，导出也未包括 probe/evidence。

## 优先级

- P1：修复提示打断理解反馈。保存当前问题标识、提问目标和求助状态，而不是仅检查最后一条 AI 消息是否带 probe。区分提示后完成与尚未验证的独立运用。
- P1：让理解判断有依据。当前 probe 只有类型和目标，没有预先制定的关键推理、可接受差异、典型误解；evidence 只验证格式与原话存在，并不验证评价本身准确。应引入判断标准和对应测试样本，不能再只追加提示词。
- P1：为长对话提供续聊。上下文可裁剪或总结，但不能把模型长度限制变成用户不能再提问；保留原始记录和未发送草稿。
- P2：清理互相矛盾的角色规则。旧提示仍要求共同改稿、导师解释后让用户指导小舟修改，后面才追加学习主线优先。建议整合为单一规则；改稿只在用户明确需要时启用。
- P2：重做学习衔接入口。按当前疑问是否已解答、用户是否希望尝试来邀请，而非按最后说话者判断；解答、尝试、求助、继续提问都保留出口。
- P2：输入优先。压缩重复标题、介绍和角色说明，让首屏能直接问；侧栏改为可选探索方向，不再强调任务完成进度。保留可折叠笔记和 AI 身份标识。
- P2：理解线索的持续性。后续上下文及导出应保留问题、原话证据、提示情况和待验证点，AI 评价仍标为暂定而非事实。

## 证据与限制

源码：lib/dialogue.ts、lib/learning.ts、app/practice-conversation.tsx、app/page.tsx、lib/expedition-session.ts。

提示后回答的模拟复现输出：理解反馈没有对应你的实际回答，本次未采用。

页面的 AI 模拟身份、资料数量、草稿保留及可选笔记是已确认的优点。当前小字与狭窄侧栏有可读性风险，未测对比度数值、读屏完整体验、移动端和键盘全流程，不能声称无障碍达标。未向真实 AI 提交用户现有草稿，也未验证本轮真实模型的提问质量。Product Design 预检脚本因 Python 命令不可用未运行成功；检查以当前项目与当前页面为依据。

建议先做学习机制修复与真实多轮用例验证，再做视觉与动画扩展。
