# 知行副本 · 首个可玩原型

## 已实现

- 校园付费自习空间案例：接收任务、三位固定人物访谈、证据标注、带教、同情境前后对照、成果与方法卡。
- 预设问题使用固定情境。自由问题由知乎直答分类后匹配固定回答；不是任意生成的角色聊天。
- 自由指导通过知乎直答分别判断中性提问 E1、证据分层 E2。成立时必须返回输入中的原文片段，服务端校验。
- 行为由规则执行，只有对应能力得到具体指导才改变。新一轮指导替换旧结果，不累计隐藏加分。
- 独立的预设指导练习入口，明确标注不调用 AI，不冒充自由输入理解。可用于接口限流时体验完整流程。
- 浏览器本地保存，包括问题草稿、笔记、指导、成果、验证计划；导出 Markdown；重新开始有确认。
- 知乎资料来源链接、作者和摘要转述标注；人物事实与真实资料分离。
- API 密钥只在服务端运行时读取，不进入前端或代码库。

## 开发

运行 `npm run dev -- --host localhost`。本机需要此前配置好的知乎 CLI 系统钥匙串。可选 `ZHIHU_CLI_PATH` 指定 CLI 文件位置。请不要在源码、命令参数或 Git 中放密钥。

生产环境的 `/api/practice` 直接调用知乎开放平台，通过 Sites secret `ZHIHU_ACCESS_SECRET` 配置。接口异常不自动重试、不替换成伪造成功。

测试：`node --experimental-strip-types --test lib/practice.test.ts lib/progress.test.ts`。

真实测试：`node scripts/test-live.mjs`，会消耗知乎额度；不要在限流期间反复运行。

## 本次验证（2026-09-08）

- 10 项自动测试通过：维度独立、固定人物、指导引用校验、非法输入、模型格式、进度恢复、损坏数据、成果确认门槛。
- TypeScript 与产品源码 lint 通过；生产构建通过。
- 本机真实知乎调用成功；真实「仅教提问」测试通过。后续真实批量测试被知乎 `rate_limit_exceeded` 中断，其他语义案例未完成验证。不能把规则单测当作模型识别效果证明。
- 尚未进行真人可用性测试或浏览器点击/视觉测试。WebMCP 注册了读取进度与确认成果工具，但无可用验证上下文，未验证其浏览器执行契约。
- 已针对审计结果升级 React 系列到 19.2.8、Vite 到 8.0.16。脚手架仍存在间接依赖审计告警，未盲目强制升级整套实验性框架。对外开放前需继续审计并增加持久化限流、接口预算控制。

## 明确边界

这是一个完整单副本原型，不是多人平台；没有跨设备同步、跨副本迁移检验、真实性能提升证据，也不承诺比赛获奖。

规则练习结果不是 AI 识别测试。真实模式仍需要 API 可用额度。知乎出处卡只基于官方检索返回摘要，需用户进入原文核对细节。

默认私有发布，仅站点所有者可访问；更改为公开必须先确认访问范围和额度风险。
